generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" // <--- CAMBIO IMPORTANTE: Antes decía "sqlite"
  url      = env("DATABASE_URL")
}

// --- ENUMS (Ahora sí son nativos en Postgres) ---
enum Role {
  ADMIN
  MEMBER
  GUEST
}

enum BookingStatus {
  PENDING
  CONFIRMED
  CANCELLED
  COMPLETED
}

// --- MODELOS ---

model User {
  id          Int       @id @default(autoincrement())
  email       String    @unique
  password    String    // <--- AGREGAR ESTO (Guardará la contraseña encriptada)
  firstName   String
  lastName    String
  phoneNumber String
  role        Role      @default(MEMBER)
  bookings    Booking[]
  fixedBookings FixedBooking[]
}

model Club {
  id          Int     @id @default(autoincrement())
  name        String
  address     String
  contactInfo String
  courts      Court[]
}

model Court {
  id                 Int            @id @default(autoincrement())
  name               String
  isIndoor           Boolean
  surface            String
  isUnderMaintenance Boolean        @default(false)
  
  clubId             Int
  club               Club           @relation(fields: [clubId], references: [id])
  
  activities         ActivityType[]
  bookings           Booking[]
  fixedBookings FixedBooking[]
}

model ActivityType {
  id                     Int       @id @default(autoincrement())
  name                   String
  description            String
  defaultDurationMinutes Int
  courts                 Court[]
  bookings               Booking[]
  fixedBookings FixedBooking[]
}

model Booking {
  id        Int           @id @default(autoincrement())
  startDateTime DateTime
  endDateTime   DateTime
  cancelledBy   Int?
  cancelledAt   DateTime?
  price     Float
  createdAt DateTime      @default(now())
  status    BookingStatus @default(PENDING) // <--- Enum nativo
  // Permitir reservas de invitados: userId opcional + guestIdentifier
  userId    Int?
  user      User?    @relation(fields: [userId], references: [id])
  guestIdentifier String?

  courtId   Int
  court     Court    @relation(fields: [courtId], references: [id])

  activityId Int
  activity   ActivityType @relation(fields: [activityId], references: [id])

  fixedBooking   FixedBooking? @relation(fields: [fixedBookingId], references: [id])
  fixedBookingId Int?
}

model FixedBooking {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  
  // Datos de la regla
  dayOfWeek Int      // 0 = Domingo, 1 = Lunes, etc.
  startTime String   // Ej: "20:00"
  endTime   String   // Ej: "21:30"
  startDate DateTime // Desde cuándo empieza el fijo

  // Relaciones
  user      User     @relation(fields: [userId], references: [id])
  userId    Int
  court     Court    @relation(fields: [courtId], references: [id])
  courtId   Int
  activity  ActivityType @relation(fields: [activityId], references: [id])
  activityId Int

  // Todas las reservas hijas generadas por este fijo
  bookings  Booking[]
}

